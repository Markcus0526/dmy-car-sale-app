# Runtime-concatenated strings — merge these at port time

Generated by `tools/extract-strings/extract.mjs`. Do not edit by hand.

Each group below is **one sentence assembled at runtime from fragments**.
Translating the fragments independently cannot work in general: word order
differs between languages, so a layout that reads correctly in Chinese
reorders wrongly in English.

The port must replace each group with a **single interpolated key**, e.g.

```
"     " + year + "年 " + quarter + "任务汇总"
    ->  t("quarterTarget.title", { year, quarter })
    en: "{{year}} {{quarter}} Target Summary"
    zh: "{{year}}年 {{quarter}}任务汇总"
```

59 call site(s), 18 unique fragment(s).

## FrmActionHis.cs:51

```csharp
logtext += "序    号：" + actionlist[i].vin + "\r\n";
```

Fragments: `actionHis.actionlist` = "序    号："

## FrmActionHis.cs:53

```csharp
logtext += "流 转 号：" + actionlist[i].chagneid + "\r\n";
```

Fragments: `actionHis.actionlist_2` = "流 转 号："

## FrmActionHis.cs:54

```csharp
logtext += "操    作：" + actionlist[i].actionkind + "		";
```

Fragments: `actionHis.actionlist_3` = "操    作："

## FrmActionHis.cs:55

```csharp
logtext += "操作时间：" + actionlist[i].actiondate + "\r\n";
```

Fragments: `actionHis.actionlist_4` = "操作时间："

## FrmActionHis.cs:56

```csharp
logtext += "批 复 人：" + actionlist[i].settlementname + "		";
```

Fragments: `actionHis.actionlist_5` = "批 复 人："

## FrmActionHis.cs:57

```csharp
logtext += "经 手 人：" + actionlist[i].handlername + "\r\n";
```

Fragments: `actionHis.actionlist_6` = "经 手 人："

## FrmActionHis.cs:58

```csharp
logtext += "当前库位：" + actionlist[i].storeplace + "\r\n";
```

Fragments: `actionHis.actionlist_7` = "当前库位："

## FrmActionHis.cs:60

```csharp
logtext += "备    注：" + actionlist[i].remark + "\r\n";
```

Fragments: `actionHis.actionlist_8` = "备    注："

## FrmReportOnroadDetail.cs:79

```csharp
rDate.Text = "从：" + startDate.ToShortDateString() + " - 到：" + endDate.ToShortDateString();
```

Fragments: `common.rDate` = "从：", `common.rDate_2` = " - 到："

## FrmReportOnroadDetail.cs:122

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportOnroadDetail.cs:124

```csharp
rTable.Cells[rowIdx, 8].Text = "小计 : " + String.Format("{0:########0}", subTotalPrice);
```

Fragments: `common.subtotalLabel` = "小计 : "

## FrmReportOnroadDetail.cs:167

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportOnroadDetail.cs:169

```csharp
rTable.Cells[rowIdx, 8].Text = "小计 : " + String.Format("{0:########0}", subTotalPrice);
```

Fragments: `common.subtotalLabel` = "小计 : "

## FrmReportOnroadDetail.cs:177

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportOnroadDetail.cs:179

```csharp
rTable.Cells[rowIdx, 8].Text = "总计 ：" + String.Format("{0:########0}", totalPrice);
```

Fragments: `common.totalLabel` = "总计 ："

## FrmReportProfitDetail.cs:79

```csharp
rDate.Text = "从：" + startDate.ToShortDateString() + " - 到：" + endDate.ToShortDateString();
```

Fragments: `common.rDate` = "从：", `common.rDate_2` = " - 到："

## FrmReportProfitDetail.cs:124

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportProfitDetail.cs:175

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportProfitDetail.cs:187

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportReserveSale.cs:75

```csharp
rDate.Text = "打印日期：" + DateTime.Now.ToShortDateString();
```

Fragments: `common.rDate_3` = "打印日期："

## FrmReportReserveSale.cs:126

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportReserveSale.cs:189

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportReserveSale.cs:202

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportSaleCountTotal.cs:80

```csharp
rDate.Text = "从：" + startDate.ToShortDateString() + " - 到：" + endDate.ToShortDateString();
```

Fragments: `common.rDate` = "从：", `common.rDate_2` = " - 到："

## FrmReportSaleCountTotal.cs:123

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + nSaleCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportSaleCountTotal.cs:177

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + nSaleCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportSaleCountTotal.cs:190

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportSaleTotal.cs:75

```csharp
rDate.Text = "打印日期：" + DateTime.Now.ToShortDateString();
```

Fragments: `common.rDate_3` = "打印日期："

## FrmReportSaleTotal.cs:126

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportSaleTotal.cs:192

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportSaleTotal.cs:208

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportStoreCarTypeDetail.cs:79

```csharp
rDate.Text = "从：" + startDate.ToShortDateString() + " - 到：" + endDate.ToShortDateString();
```

Fragments: `common.rDate` = "从：", `common.rDate_2` = " - 到："

## FrmReportStoreCarTypeDetail.cs:122

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreCarTypeDetail.cs:124

```csharp
rTable.Cells[rowIdx, 8].Text = "小计 : " + String.Format("{0:########0}", subTotalPrice);
```

Fragments: `common.subtotalLabel` = "小计 : "

## FrmReportStoreCarTypeDetail.cs:164

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreCarTypeDetail.cs:166

```csharp
rTable.Cells[rowIdx, 8].Text = "小计 : " + String.Format("{0:########0}", subTotalPrice);
```

Fragments: `common.subtotalLabel` = "小计 : "

## FrmReportStoreCarTypeDetail.cs:174

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportStoreCarTypeDetail.cs:176

```csharp
rTable.Cells[rowIdx, 8].Text = "总计 ：" + String.Format("{0:########0}", totalPrice);
```

Fragments: `common.totalLabel` = "总计 ："

## FrmReportStoreCartypeTotal.cs:74

```csharp
rDate.Text = "打印日期：" + DateTime.Now.ToShortDateString();
```

Fragments: `common.rDate_3` = "打印日期："

## FrmReportStoreCartypeTotal.cs:113

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + String.Format("{0:########0}", subTotalPrice);
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreCartypeTotal.cs:146

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + subTotalPrice;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreCartypeTotal.cs:154

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportStoreDetail.cs:79

```csharp
rDate.Text = "从：" + startDate.ToShortDateString() + " - 到：" + endDate.ToShortDateString();
```

Fragments: `common.rDate` = "从：", `common.rDate_2` = " - 到："

## FrmReportStoreDetail.cs:122

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreDetail.cs:124

```csharp
rTable.Cells[rowIdx, 8].Text = "小计 : " + String.Format("{0:########0}", subTotalPrice);
```

Fragments: `common.subtotalLabel` = "小计 : "

## FrmReportStoreDetail.cs:164

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreDetail.cs:166

```csharp
rTable.Cells[rowIdx, 8].Text = "小计 : " + String.Format("{0:########0}", subTotalPrice);
```

Fragments: `common.subtotalLabel` = "小计 : "

## FrmReportStoreDetail.cs:174

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportStoreDetail.cs:176

```csharp
rTable.Cells[rowIdx, 8].Text = "总计 ：" + String.Format("{0:########0}", totalPrice);
```

Fragments: `common.totalLabel` = "总计 ："

## FrmReportStoreTotal.cs:74

```csharp
rDate.Text = "打印日期：" + DateTime.Now.ToShortDateString();
```

Fragments: `common.rDate_3` = "打印日期："

## FrmReportStoreTotal.cs:117

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreTotal.cs:160

```csharp
rTable.Cells[rowIdx, 1].Text = "数量 : " + rowCount;
```

Fragments: `common.quantityLabel` = "数量 : "

## FrmReportStoreTotal.cs:169

```csharp
rTable.Cells[rowIdx, 1].Text = "总数量 : " + totalCount;
```

Fragments: `common.totalQuantityLabel` = "总数量 : "

## FrmReportWholeSaleTotal.cs:80

```csharp
rDate.Text = "从：" + startDate.ToShortDateString() + " - 到：" + endDate.ToShortDateString();
```

Fragments: `common.rDate` = "从：", `common.rDate_2` = " - 到："

## FrmStatisCarSaleQuarter.cs:159

```csharp
gridQuarter.Rows[2][0] = tsbQuarter.SelectedIndex * 3 + 1 + "月";
```

Fragments: `statisCarSaleQuarter.Rows_2` = "月"

## FrmStatisCarSaleQuarter.cs:160

```csharp
gridQuarter.Rows[3][0] = tsbQuarter.SelectedIndex * 3 + 2 + "月";
```

Fragments: `statisCarSaleQuarter.Rows_2` = "月"

## FrmStatisCarSaleQuarter.cs:161

```csharp
gridQuarter.Rows[4][0] = tsbQuarter.SelectedIndex * 3 + 3 + "月";
```

Fragments: `statisCarSaleQuarter.Rows_2` = "月"

## FrmStatisCarSaleQuarter.cs:810

```csharp
tsbTitleText.Text = "     " + tsbYear.SelectedItem.ToString() + "年 " + tsbQuarter.SelectedItem.ToString() + "任务汇总";
```

Fragments: `statisCarSaleQuarter.tsbTitleText` = "年 ", `statisCarSaleQuarter.tsbTitleText_2` = "任务汇总"

## FrmStatisCarSaleQuarter.cs:837

```csharp
tsbTitleText.Text = "     " + tsbYear.SelectedItem.ToString() + "年 " + tsbQuarter.SelectedItem.ToString() + "任务汇总";
```

Fragments: `statisCarSaleQuarter.tsbTitleText` = "年 ", `statisCarSaleQuarter.tsbTitleText_2` = "任务汇总"
